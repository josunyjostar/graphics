renderTarget - 렌더링의 대상이 되는 타겟
viewport - 스크린의 어디에 그릴지 지정해주는 것
inputLayout - vertex 버퍼로 어떤 데이터가 들어갈지 지정해주는 것
bitonic sort - 배열의 사이즈 개수가 2의 제곱이여야한다는 제약이 있음
view - directx에서 view는 자원을 어떻게 해석하고 사용할지에 대한 관점을 말함 ex) ShaderResourceView

hlsl은 디폴트로 colunm major를 사용
때문에 directx에서는 row major로 계산하고 GPU로 보낼때 tanspose해서 보낸다

shader의 SV_TARGET 의 결과가 렌더타겟에 저장이 된다.

input assemble - 정점을 모아서 삼각형 단위로 만들어줌 
vertex shader - 모델,뷰변환 projection

vertex 좌표에서 texcoord를 역산 할 수 가 있음 (20면체 코드 볼 것)

hull shader
tessellation stage
domain shader
geometry shader - 삼각형 하나를 10,20등으로 나눠서 상세도를 높임

rasterization - 삼각형 단위로 바운딩 박스 내부를 보간해서 픽셀로 만들어줌 , 뷰포트도 여기서 설정 
(뷰포트란? 스크린의 어디에 어떻게 그릴지설정)
pixel shader - 픽셀 단위로 컬러값을 결정

output merger - 깊이 버퍼링(중요), 블렌딩


2차원 벡터에 수직하는 벡터는
(x,y) -> (y,-x)

f = ma
a = f/m

도함수 = 미분된 함수 결과값은 미분
시간 * 속도 = 거리
거리 / 시간 = 속도 
속도 / dt = 가속도
미분 = 변화 (순간변화율)

밀도 = 질량 / 부피
압력 = 힘 / 면적 

스피드 = 속도의 절대값 = normalize(velocity)

압력을 밀도로 나누는것은 속도와 연관이 있음

스칼라에 나블라연산은 그레디언트(벡터)가 나옴
gradiant     함수의 가장 큰 방향과 크기 (벡터)

divergence 발산 밀도를 유지하냐 못하냐를 divergence로 측정 가능 (스칼라)
벡터장에 영향받는 파티클의 움직임을(밀도를) 묘사할 수 있음
유체의 압축 또는 팽창: 
발산이 양수인 경우: 
해당 지점에서 유체가 공간적으로 퍼져 나가고 있다는 것을 의미합니다. 이는 유체가 팽창하고 있음을 나타냅니다.
발산이 음수인 경우: 해당 지점에서 유체가 공간적으로 모여들고 있다는 것을 의미합니다. 이는 유체가 압축되고 있음을 나타냅니다.
발산이 0인 경우: 해당 지점에서 유체의 순수한 퍼짐이나 모임이 없다는 것을 의미합니다. 
(들어온거랑 나가는 양이 같음)
이는 유체가 비압축성(incompressible)임을 나타냅니다.
어떤 속도장의 divergence = 0 이라는건 비압축성 조건을 만족한다는 뜻

laplacian    반환값은 스칼라 그레디언트의 발산을 의미 /
 f(x,y) = x³ + y³ 함수의 라플라시안  ∇²f = 6x + 6y
각 지점에서 함수의 변화율을 알려준다 / 각각의 지점이 어느 정도의 발산을 하는지를 알려준다

질량이 일정한데 부피를 유지하는것은 밀도를 유지하는 것과 같은 말
= 비압축성 (imcompressibility)

겉미분 속미분 복습 필요
https://www.youtube.com/watch?v=BRJ05c5cpwM&ab_channel=%ED%8C%8C%EA%B9%A8%EB%B9%84TV

샘 추천 영상 Simulating Fluids
https://www.youtube.com/watch?v=rSKMYc1CQHE

논문에서 discretization는 delta를 말하는듯
spatial discretization = delta pos(x,y,z)